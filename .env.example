WEB_PORT="8080"
API_PORT="3001"
DB_PORT="5432"
VITE_API_BASE_URL="http://localhost:3001"
VITE_API_PORT="3001"
VITE_PUBLIC_SITE_URL="http://localhost:8080"
# Development-only login prefill for the frontend login form.
# NEXT_PUBLIC_* takes precedence over VITE_* when both are defined.
NEXT_PUBLIC_DEV_LOGIN_EMAIL=""
NEXT_PUBLIC_DEV_LOGIN_PASSWORD=""
VITE_DEV_LOGIN_EMAIL=""
VITE_DEV_LOGIN_PASSWORD=""
AUTH_STORAGE_MODE="postgres"
JWT_ACCESS_SECRET="change-me-access-secret"
JWT_REFRESH_SECRET="change-me-refresh-secret"
POSTGRES_DB="agilys"
POSTGRES_USER="agilys_app"
POSTGRES_PASSWORD="change-me-local-only"
POSTGRES_PORT="5432"
# Backend test account defaults remain configured separately.
# Use AUTH_TEST_USER_EMAIL and AUTH_TEST_USER_PASSWORD in your local backend env if needed.
